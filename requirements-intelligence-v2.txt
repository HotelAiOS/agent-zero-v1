# requirements.intelligence-v2.txt
"""
Agent Zero V1 - Intelligence V2.0 Package Requirements
Production-ready dependencies with version pinning for stability

CRITICAL: Compatible with existing Agent Zero V1 requirements
"""

# === CORE DEPENDENCIES ===
# FastAPI ecosystem
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# === ASYNC AND CONCURRENCY ===
asyncio-mqtt==0.13.0
aiohttp==3.9.1
aiofiles==23.2.1

# === DATABASE AND STORAGE ===
# SQLite (built-in with Python)
sqlite3  # Built-in
neo4j==5.15.0
redis==5.0.1

# === AI AND MACHINE LEARNING ===
# Lightweight ML for priority calculation
scikit-learn==1.3.2
numpy==1.24.4
pandas==2.0.3

# Optional: Enhanced NLP (graceful fallback if not available)
# spacy==3.7.2
# sentence-transformers==2.2.2

# === MONITORING AND OBSERVABILITY ===
prometheus-client==0.19.0
logging-tree==1.9

# === UTILITY LIBRARIES ===
python-dateutil==2.8.2
pytz==2023.3
click==8.1.7

# === TESTING (DEV) ===
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing FastAPI

# === EXISTING AGENT ZERO COMPATIBILITY ===
# Ensure compatibility with existing requirements
requests==2.31.0
python-dotenv==1.0.0

# === PRODUCTION DEPLOYMENT ===
gunicorn==21.2.0

# === SECURITY ===
cryptography==41.0.8

# === DEVELOPMENT TOOLS (OPTIONAL) ===
# black==23.11.0
# flake8==6.1.0
# mypy==1.7.1

# === DOCKER OPTIMIZATION ===
# Keep image size minimal by using slim Python base image
# and only installing essential dependencies